./evaluate.sh
Prend un fichier de requêtes en argument et applique main.lua successivement avec
toutes les stratégies (inter, intra, both, et singletons). La sortie est
redirigée vers un fichier nommé automatiquement à partir du fichier d'entrée et
de la stratégie utilisée.

./main.lua
Prend en argument le nom d'une stratégie. Lis sur l'entrée standard un ensemble
de requêtes et écris sur la sortie standard les commandes générées pour chaque
entrée, accompagnées d'autres informations.
Les paramètres concernant la localisation de la base de cas sont à définir À
L'INTÉRIEUR de main.lua : key_file et value_file respectivement pour le fichier
de requêtes et celui de commandes.

./generate_triples.lua
Génère la liste de toutes les combinaisons possibles formées à partir de trois
éléments du fichier key_file (variable définie dans le code). Les combinaisons
sont formées uniquement pour trois requêtes associées strictement à la même
commande.

./solve_inline_triple.lua
Prend un triplet en argument (séparés par des tabulations mais en un seul
argument) et résoud l'équation analogique associée. 
